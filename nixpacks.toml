# Railway / Nixpacks config — minimal Node-only build.
# PDF rendering is handled by a placeholder fallback; no Chromium needed.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"
