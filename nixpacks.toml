# Railway / Nixpacks config
# @sparticuz/chromium bundles its own Chrome — no system-wide install needed.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"
